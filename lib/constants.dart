


import 'package:flutter/material.dart';

const String kThemeModeKey = 'themeModeKey';


ThemeData kDarkModeTheme = ThemeData(
  primaryColor: Colors.red,
      appBarTheme: AppBarTheme(
        backgroundColor: Colors.red,
      )

);